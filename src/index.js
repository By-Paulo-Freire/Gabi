import React from 'react';
import ReactDOM from 'react-dom/client';
import Gabi from './Gabi';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <Gabi />
  </React.StrictMode>
);

